/**
 * En el mundo de la programación existen lenguajes de tipado dinamico
 * y los de tipado estactico este último exigiendo al momento de declarar 
 * una variable el tipo de dato  
*/

let numeros = 1; // number
let texto = "Variable de Tipo Texto."; // str 
let verdadero = true; // boolean
let falso = false; // boolean
let noDefinido; // undefined
let noDef = undefined; // undefined puede ser un tipo de dato o el valor de una variable
let nulo = null; // null 

console.log(typeof numeros);

numeros = "uno"

console.log(typeof numeros);


console.log(typeof verdadero);

verdadero = "true"

console.log(typeof verdadero);
